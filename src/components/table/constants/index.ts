import type { AlignType } from "../types";

export const DEFAULT_PAGE_SIZE = 10;

export const ALIGN_CLASS: Record<AlignType, string> = {
	left: "text-left",
	center: "text-center",
	right: "text-right",
};
